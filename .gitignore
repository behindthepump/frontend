node_modules/
dist/
coverage/

# Firebase CLI local deploy cache
.firebase/

# local env (secrets/config) — keep the example + committed production config
.env*
!.env.example
!.env.production

.DS_Store
*.log
